# spec.md — ひとり営業用 顧客管理アプリ 仕様書

## 1. 目的・利用シーン

### 目的

Excelの顧客台帳を置き換える、ひとり営業向けの最小構成CRM。ブラウザ1枚で顧客と商談を管理し、見込み→提案→成約のパイプラインを可視化する。

### 利用シーン

| シーン | 操作 |
|---|---|
| 新規顧客との名刺交換後 | 顧客を登録し、初回商談を追加する |
| 商談進捗の確認 | パイプラインビューで全商談の状況を俯瞰する |
| 商談ステータスの更新 | パイプラインの「→」ボタンで次ステージに進める |
| 顧客情報の参照 | 顧客ビューで会社名・担当者・連絡先を確認する |
| 訪問前の準備 | 顧客詳細のフォローアップメモを見直す |

---

## 2. データ項目

### 顧客（`crm-customers`）

| フィールド | 型 | 必須 | 備考 |
|---|---|---|---|
| `id` | string | ○ | `"c_" + Date.now()` で自動生成 |
| `company` | string | ○ | 会社名（例: 株式会社サンプル） |
| `name` | string | ○ | 担当者名（例: 山田 太郎） |
| `title` | string | — | 役職（任意） |
| `email` | string | — | メールアドレス（任意） |
| `phone` | string | — | 電話番号（任意） |
| `memo` | string | — | メモ（任意、複数行可） |
| `createdAt` | string | ○ | ISO 8601形式、自動付与 |

### 商談（`crm-deals`）

| フィールド | 型 | 必須 | 備考 |
|---|---|---|---|
| `id` | string | ○ | `"d_" + Date.now()` で自動生成 |
| `customerId` | string | ○ | 顧客の `id` を格納（1対多の紐付け） |
| `dealTitle` | string | ○ | 商談タイトル（例: サービスA導入提案） |
| `amount` | number | — | 金額（整数・円、任意） |
| `status` | string | ○ | `"lead"` / `"proposal"` / `"won"` の3値 |
| `followUpMemo` | string | — | フォローアップメモ（任意、複数行可） |
| `createdAt` | string | ○ | ISO 8601形式、自動付与 |
| `updatedAt` | string | ○ | ISO 8601形式、保存のたびに更新 |

### 1対多の関係

```
顧客（1） ─── 商談（多）
  id  ←────── customerId
```

1人の顧客に対して0件以上の商談を持つ。商談は必ず1件の顧客に属する。

---

## 3. 画面構成

### タブ切替（上部）

```
[ 顧客 ] [ パイプライン ]
```

どちらか一方が常にアクティブ。アクティブタブはアクセントカラーでアンダーライン表示。

---

### ビュー① 顧客（ツーカラムレイアウト）

```
┌──────────────────────┬───────────────────────────────────┐
│ 左ペイン（340px固定）  │ 右ペイン（flex-1）                  │
│                      │                                   │
│ [検索ボックス]        │  ← 4種のモードを切り替えて表示      │
│ [新規顧客ボタン]      │                                   │
│                      │                                   │
│ ┌──────────────────┐ │                                   │
│ │ 顧客カード        │ │                                   │
│ │ 会社名           │ │                                   │
│ │ 担当者名         │ │                                   │
│ └──────────────────┘ │                                   │
│ ┌──────────────────┐ │                                   │
│ │ 顧客カード        │ │                                   │
│ └──────────────────┘ │                                   │
└──────────────────────┴───────────────────────────────────┘
```

#### 右ペインの4モード

| モード名 | `data-pane` 値 | 表示内容 |
|---|---|---|
| 空状態 | `empty` | 「顧客を選択してください」などの案内 |
| 顧客詳細 | `detail` | 連絡先ブロック＋商談一覧＋「商談を追加」ボタン＋編集・削除ボタン |
| 顧客フォーム | `customer-form` | 顧客の入力欄（新規・編集共通）＋保存・キャンセルボタン |
| 商談フォーム | `deal-form` | 商談の入力欄（新規・編集共通）＋保存・削除・キャンセルボタン |

- 選択中の顧客カードはハイライト（左ボーダーにアクセントカラー）表示する。
- 商談一覧の各行にはステータスバッジを表示する。行クリックで商談フォーム（編集）に遷移。
- 検索は会社名・担当者名・役職を横断検索する（リアルタイムフィルタ）。
- 顧客の並び順は `createdAt` の新しい順。

---

### ビュー② パイプライン（カンバン3列）

```
┌────────────┬────────────┬────────────┐
│  見込み     │  提案       │  成約       │
│  (lead)    │ (proposal) │  (won)     │
│            │            │            │
│ ┌────────┐ │ ┌────────┐ │ ┌────────┐ │
│ │商談カード│ │ │商談カード│ │ │商談カード│ │
│ │会社名  │ │ │会社名  │ │ │会社名  │ │
│ │金額    │ │ │金額    │ │ │金額    │ │
│ │      →│ │ │←    →│ │ │←      │ │
│ └────────┘ │ └────────┘ │ └────────┘ │
└────────────┴────────────┴────────────┘
```

- 各列ヘッダーに列名とカード件数バッジを表示する。
- カードの左に3pxのアクセントライン（ステータス色）を付ける。
- 「←」ボタン: 先頭列（見込み）では非表示。クリックで前のステータスに移動。
- 「→」ボタン: 末尾列（成約）では非表示。クリックで次のステータスに移動。
- カード本体クリック → 顧客ビューに切替 → 該当顧客を選択 → 商談フォーム（編集）を開く。

---

## 4. CRUD操作フロー

### 顧客 CRUD

| 操作 | トリガー | 遷移先 |
|---|---|---|
| Create | 「新規顧客」ボタン → 顧客フォーム（空）→ 保存 | 顧客詳細（新規顧客） |
| Read（一覧） | 起動時 / 検索入力 | 左ペインに顧客カード一覧を表示 |
| Read（詳細） | 顧客カードをクリック | 右ペイン → 顧客詳細モード |
| Update | 顧客詳細 → 「編集」ボタン → 顧客フォーム（既存値入り）→ 保存 | 顧客詳細（更新後） |
| Delete | 顧客詳細 → 「削除」ボタン → 確認ダイアログ → OK | 空状態（紐付く商談も連鎖削除） |

### 商談 CRUD

| 操作 | トリガー | 遷移先 |
|---|---|---|
| Create | 顧客詳細 → 「商談を追加」→ 商談フォーム（空）→ 保存 | 顧客詳細（商談一覧に追加） |
| Read（一覧） | 顧客詳細表示時 | 顧客詳細の商談一覧セクション |
| Read（カンバン） | パイプラインビュー | 3列にステータス別表示 |
| Update | 商談行クリック → 商談フォーム（既存値入り）→ 保存 | 顧客詳細（商談一覧に反映） |
| Delete | 商談フォーム → 「削除」ボタン → 確認ダイアログ → OK | 顧客詳細（商談一覧から削除） |
| ステータス移動 | パイプラインの「←」「→」ボタン | パイプライン（カード列が即座に移動） |

---

## 5. 永続化方針

### localStorageキー

| キー | 保存形式 | 読み書きタイミング |
|---|---|---|
| `crm-customers` | JSON文字列（配列） | 顧客のCreate/Update/Delete時に全件上書き |
| `crm-deals` | JSON文字列（配列） | 商談のCreate/Update/Delete/ステータス移動時に全件上書き |

### 保存・読み込みパターン

```
読み込み: JSON.parse(localStorage.getItem('crm-customers') || '[]')
書き込み: localStorage.setItem('crm-customers', JSON.stringify(customers))
```

### 顧客削除時の連鎖削除ルール

1. 対象顧客の `id` を取得する。
2. `crm-deals` から `customerId === id` の商談をすべて除外した配列を作成する。
3. `crm-deals` と `crm-customers` を同時に更新する（両方の書き込みが完了してから画面を更新する）。

---

## 6. 初期データ

### 投入タイミング

アプリ起動時に `crm-customers` が `null` または空配列のときのみ初期データを投入する。以降は投入しない。

### 初期データ内容

**顧客 3件**（実在しない会社名・人物名を使用）

| 会社名 | 担当者名 | 役職 |
|---|---|---|
| 株式会社アルファテック | 佐藤 健一 | 情報システム部長 |
| ベータ商事株式会社 | 鈴木 美咲 | 購買課長 |
| ガンマコンサルティング合同会社 | 田中 洋介 | 代表社員 |

**商談 5件**（3つのステータスに分散）

| 商談タイトル | 顧客 | ステータス | 金額 |
|---|---|---|---|
| クラウド移行支援サービス提案 | 株式会社アルファテック | `proposal`（提案） | 1,200,000円 |
| 社内DXツール導入相談 | 株式会社アルファテック | `lead`（見込み） | — |
| 在庫管理システム刷新 | ベータ商事株式会社 | `won`（成約） | 3,500,000円 |
| 経営コンサルティング契約 | ガンマコンサルティング合同会社 | `proposal`（提案） | 800,000円 |
| ウェブサイト改善コンサル | ガンマコンサルティング合同会社 | `lead`（見込み） | 250,000円 |

---

## 7. 対象外の機能

以下は今回のスコープに含めない。

| 機能 | 理由・代替 |
|---|---|
| ドラッグ＆ドロップでのステータス移動 | 「←」「→」ボタンで代替 |
| クラウド保存・他端末共有 | localStorageのみ対応 |
| ログイン・マルチユーザー | シングルユーザー前提 |
| CSV/Excelインポート・エクスポート | 対象外 |
| モバイル対応（レスポンシブ） | PC表示のみ |
| メール送信連携 | 対象外 |
| カレンダー連携 | 対象外 |
| 商談の期限・リマインダー | 対象外 |
| 売上集計・レポート | 対象外 |
